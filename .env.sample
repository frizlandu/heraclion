# SAMPLE ENVIRONMENT VARIABLES for Heraclion
# Do NOT store real secrets in this file. Replace placeholder values in your hosting provider.

########### BACKEND (Render) ###########
# NODE environment
NODE_ENV=production

# Postgres connection (replace with your managed DB values)
DB_HOST=your-postgres-host.example.com
DB_PORT=5432
DB_NAME=heraclion_prod
DB_USER=heraclion_user
DB_PASSWORD=changeme
DB_SSL=true

# JWT secret - generate a secure random value
JWT_SECRET=base64_or_random_secret_here

# Optional SMTP for emails
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=smtp_user
SMTP_PASS=smtp_password

# CORS - frontend origin(s) allowed (comma-separated)
CORS_ALLOWED_ORIGINS=https://your-frontend.vercel.app

# File storage (optional) - use S3 or equivalent in production
S3_BUCKET=heraclion-uploads
S3_ACCESS_KEY=AKIA...
S3_SECRET_KEY=abcd1234
S3_REGION=us-east-1

# App-specific
PORT=3000

########### FRONTEND (Vercel) ###########
# React Create App env vars must start with REACT_APP_
REACT_APP_API_BASE_URL=https://your-backend-service.onrender.com
REACT_APP_WS_URL=wss://your-backend-service.onrender.com
REACT_APP_ENV=production
